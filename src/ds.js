SET_SELECT_VALUE:
  selectValue

SET_SITE_INPUT_VALUE:
  inputValue

SET_RADIO_INPUTS:
  radioInputs,
  requiredOptions,
  optionalOptions,
  preparedValue

SET_REQUIRED_OPTIONS_INPUT_VALUE:
  requiredOptions

SET_OPTIONAL_OPTIONS_INPUT_VALUE:
  optionalOptions

SET_RESULT_LINK:
  resultLink,
  selectValue,
  inputValue,
  requiredOptions,
  optionalOptions,
  inputPlaceholder

SET_COPY_LINK:
  isCopied

SET_MODAL_CONTENT:
  modalContent,
  requiredOptions,
  optionalOptions

SET_SHOW_MODAL:
  showModal